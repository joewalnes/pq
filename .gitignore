/target
/data
*.parquet
!tests/fixtures/test_data.parquet
/docs/build
__pycache__/
/dist
.wrangler/

# Generated by `make licenses` (cargo-about + NOTICE appendix): ~525KB,
# tracks Cargo.lock exactly, regenerate at build/release time rather than
# committing a copy that goes stale on every dependency bump. Matches at
# the repo root and in every npm/<pkg>/ directory (all 5 copies are
# identical, `make licenses` just fans the one generated file out).
THIRD-PARTY-LICENSES
